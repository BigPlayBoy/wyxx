<div class="bd">

    <ul>
        {{each $data}}
        <li><a href="#"><img src="{{$value.url}}" alt="">
                <p>{{$value.title}}</p>
            </a></li>
            {{/each}}
        
    </ul>
</div>