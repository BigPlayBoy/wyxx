<div class="hd clearfix">
    <h2>新鲜甩尾</h2>
    <em>每日不可不看的好货</em>
</div>
<div class="bd">

    <ul>
        {{each $data}}
        <li><a href="#"><img src="{{$value.url}}" alt="">
                <p>{{$value.title}}</p>
            </a></li>
            {{/each}}
        
    </ul>
</div>